By default, the app will sort every .c and .cpp file in
the current directory non-recursively if no parameter is specified.

Additional parameters:

-h			-> Displays the help text
-r			-> Sorts recursively (includes subdirectories)
-p <path>	-> Specifies a path directory